document.addEventListener('DOMContentLoaded', function () {
    const swiperContainer = document.querySelector('.swiper-container');
    const nextButton = document.querySelector('.custom-button-next');
    const prevButton = document.querySelector('.custom-button-prev');

    if (swiperContainer && nextButton && prevButton) {
        // Tu código para manejar los botones y la funcionalidad de Swiper
    } else {
        console.error('Los elementos no están presentes en el DOM.');
    }
});
